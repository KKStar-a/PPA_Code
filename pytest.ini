[pytest]
testpaths = tests
# Reduce dependence on unrelated system-level pytest plugins (e.g. ROS stacks).
addopts = -p no:launch_testing -p no:launch_pytest -p no:pytest_ros -p no:ament_copyright -p no:ament_flake8 -p no:ament_pep257
